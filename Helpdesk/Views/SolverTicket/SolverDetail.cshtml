@using Models.Tickets
@model (Ticket, SolverHistory)
@{
    ViewData["Title"] = "Detail změny řešitele";
}

<div class="d-flex flex-row justify-content-center">
    <div class="mx-2">
        @await Html.PartialAsync("~/Views/SolverTicket/Components/SolverChangeDetailPartial.cshtml", (Model.Item1.Id, Model.Item2))
    </div>
    <div class="mx-2">
        @await Html.PartialAsync("~/Views/SolverTicket/Components/TicketSolverPartial.cshtml", Model.Item1)
    </div>
</div>
